# Credentials / secrets — never commit
secrets.h
**/secrets.h
# (the committed template secrets.example.h is intentionally NOT ignored)

# Arduino build output
build/
*.bin
*.elf
*.hex
*.map
**/.DS_Store