{
  "database": { "rules": "firebase-rules.json" },
  "firestore": { "rules": "firestore.rules" },
  "emulators": {
    "database": { "port": 9000 },
    "firestore": { "port": 8080 },
    "singleProjectMode": true,
    "ui": { "enabled": false }
  }
}
