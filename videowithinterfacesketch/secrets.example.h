#pragma once
// -----------------------------------------------------------------------------
// Template for secrets.h — COPY this file to "secrets.h" in the same folder and
// fill in your real values. secrets.h is gitignored; this template is committed.
// -----------------------------------------------------------------------------
// 2.4 GHz Wi-Fi (secondary / bench link)
#define WIFI_SSID     "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"

// Wi-Fi HaLow (primary link) — the SSID/password set on the HT-H7608 gateway's AP
#define HALOW_SSID     "your-halow-ap-name"
#define HALOW_PASSWORD "your-halow-password"
